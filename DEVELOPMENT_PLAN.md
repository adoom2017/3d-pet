# DesktopPet 开发计划

DesktopPet 是面向 Windows 和 macOS 的轻量级 3D 桌面宠物。它以透明、置顶的小窗口承载 glTF 2.0 / GLB 模型，让宠物能够播放骨骼动画、在桌面移动并响应鼠标交互。

本文只描述项目目标、MVP 边界、技术路线与里程碑。编码契约见 [ARCHITECTURE.md](ARCHITECTURE.md)，逐阶段执行与验收门禁见 [TASKS.md](TASKS.md)。

## 1. 项目原则

- 先完成稳定、低资源占用、可长期运行的桌宠引擎，再扩展内容和智能能力。
- MVP 之前不开发 AI；AI 未来只能提交意图，不能直接控制窗口、动画或渲染器。
- macOS 是 MVP 的主要运行、视觉、交互和性能验收平台；Windows 保留源码支持，并由 CI 持续验证编译、Clippy、测试和构建。
- 行为、物理、坐标转换等核心逻辑保持确定性和平台无关，平台差异集中隔离。
- 每个 Phase 必须完成自动验证、要求的平台验收并留下证据，才能解锁下一阶段。

## 2. MVP 范围

### 2.1 必须包含

- macOS 完整运行；Windows 代码路径持续保持可编译和自动测试通过。
- 320 x 320 初始逻辑尺寸的无边框、透明、不可缩放、置顶窗口。
- 加载并显示带骨骼动画的 GLB 宠物资源。
- Idle 与 Walk 循环动画，以及二者之间的 250 ms Cross Fade。
- 与帧率无关的桌面水平移动和随机 Idle / Walk / Turn 行为。
- 多显示器、负桌面坐标、DPI 转换、工作区边界夹紧和边缘转身。
- 2D 包围区域或 hit mask 命中测试。
- 透明区域鼠标穿透，宠物区域仍可点击和拖动。
- 拖动、释放、重力下落、落地以及回到 Idle 的完整状态转换。
- 在基础骨骼动画后叠加平滑且受角度限制的 Look At Mouse。
- Active 60 FPS、Idle 30 FPS、Sleep 15 FPS，并在完全静止时按事件渲染。
- 可诊断的错误和结构化日志，以及覆盖核心纯逻辑的自动测试。

### 2.2 不属于 MVP

- AI、LLM、TTS、ASR 或任何网络服务。
- 登录、云同步、商店、远程资源下载或在线更新。
- 多宠物、宠物包安装器、第三方代码插件。
- 完整刚体引擎、弹跳、攀爬、窗口平台碰撞等复杂物理。
- 桌面窗口感知、站在其他应用窗口上或操作桌面元素。
- 托盘菜单、设置界面、开机启动、自动更新、签名和公证发布流水线。

## 3. 技术路线

| 领域 | 选择 | 目的 |
| --- | --- | --- |
| 语言 | Rust | 内存安全、可预测性能、跨平台交付 |
| 窗口与事件 | winit | 窗口生命周期、输入、DPI 和显示器事件 |
| GPU 渲染 | wgpu | 跨 Metal / Direct3D 的统一渲染层 |
| 资源格式 | glTF 2.0 / GLB | 在单一资源中承载网格、材质、纹理、骨骼和动画 |
| 平台集成 | windows-rs；objc2 系列 | 隔离两端的窗口层级和动态鼠标穿透能力 |
| 序列化 | serde / serde_json | 宠物 manifest 与配置 |
| 诊断 | tracing；thiserror；anyhow | 结构化日志和分层错误上下文 |

依赖版本不在规划文档中写死。Phase 0 选择当时稳定且互相兼容的版本，由 `Cargo.toml` 声明并由 `Cargo.lock` 固定，同时记录 winit / wgpu 兼容性依据。

默认开发资产选用 Quaternius Animated Animals 中同时包含 Idle 和 Walk 的 CC0、带骨骼 GLB。资源入库时必须保存原始下载地址、作者、许可证文本或链接、获取日期、文件校验值以及实际动画名称映射；在完成这些记录前不得把资产纳入发布包。

## 4. MVP 路线图

所有阶段严格串行执行。以下仅描述阶段目标，详细任务、命令与退出条件以 [TASKS.md](TASKS.md) 为准。

| Phase | 里程碑 | 目标摘要 |
| --- | --- | --- |
| 0 | 工程基线 | 建立单应用 Cargo workspace、入口、日志、错误、测试结构和 macOS / Windows CI 矩阵 |
| 1 | 透明窗口 | 创建无边框、透明、不可缩放、置顶的 320 x 320 窗口 |
| 2 | wgpu 基线 | 初始化 GPU 与透明 surface，在窗口中绘制三角形 |
| 3 | 静态 GLB | 通过 manifest 加载并渲染网格、纹理、材质和相机 |
| 4 | 骨骼与 Idle | 实现 skin、joint matrix、动画采样和 Idle 循环 |
| 5 | 动画过渡 | 实现 Idle / Walk 名称映射、循环、速度和 250 ms Cross Fade |
| 6 | 桌面移动 | 分离桌面位置与模型位置，按时间增量移动窗口 |
| 7 | PetBrain | 用可注入随机源和时钟实现确定性的 Idle / Walk / Turn 决策 |
| 8 | 边界与显示器 | 支持工作区、负坐标、DPI、多显示器、边缘夹紧和转身 |
| 9 | 命中测试 | 统一坐标转换，区分宠物区域和透明区域 |
| 10 | 平台鼠标穿透 | 在 Windows 和 macOS 动态切换透明区域的点击穿透 |
| 11 | 拖动 | 实现命中后按下、偏移记录、跨边缘移动和释放 |
| 12 | 重力与落地 | 实现 Falling / Landing / Idle、地面夹紧和释放速度处理 |
| 13 | Look At Mouse | 在基础姿态后叠加受限且平滑的头部 yaw / pitch |
| 14 | 性能与 MVP 验收 | 实现自适应 FPS、按需渲染，完成 macOS 性能与完整场景验收及 Windows CI 兼容性验证 |

## 5. 发布里程碑

首个发布里程碑固定为：

```text
v0.1.0-desktop-pet-mvp
```

只有同时满足以下条件才能创建该 Git tag：

- Phase 0-14 全部为 `Done`，每个阶段均有日期、commit、平台、结果以及截图或日志路径。
- `cargo fmt --check`、Clippy、workspace 测试和 workspace 构建全部通过。
- GitHub Actions 的 macOS / Windows 矩阵在候选 commit 上全部为绿色。
- macOS 实机完成最终 MVP 场景：启动、透明置顶、GLB、Idle、随机 Walk、边缘转身、命中与穿透、点击、拖放、落地、Look At Mouse 和自适应 FPS。
- macOS 没有骨骼明显变形、黑色透明背景、输入被永久阻断、跨 DPI 显示器跳变或长时间 busy loop；未取得证据时不宣称 Windows 运行时行为一致。
- 正常活动时 CPU 目标低于 2%，Idle 尽可能接近零，内存低于 150 MB；测试方法、机器配置和采样结果已归档。
- 默认资产来源和 CC0 许可证记录完整，发布包只包含受信的预置资源类型。

MVP tag 之前禁止开始 AI、网络、商店、多宠物、复杂物理或桌面窗口感知的开发。

## 6. MVP 之后

后续能力按基础稳定性和用户价值逐步评估，不预先承诺版本：

1. 产品化：托盘菜单、设置、音效、开机启动、签名、公证和自动更新。
2. 内容系统：更多动作、皮肤、受校验的宠物包和资源安装流程。
3. 桌面环境：感知其他窗口、窗口平台移动和更丰富的桌面互动。
4. 高级运动：跳跃、投掷、弹跳和必要时引入受控的物理引擎。
5. 智能能力：通过 `PetIntent` 边界接入对话、语音或模型决策，失败时不影响本地基础行为。

任何后续阶段都不得破坏 MVP 已建立的平台隔离、行为确定性、资源可信边界和性能预算。
